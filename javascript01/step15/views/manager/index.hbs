<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>HTML</title>
  <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.min.css">
  <style>
  .container {
    width: 1000px;
    margin: 0px auto;
  }
  a[disabled] {
    pointer-events: none;
  }
  </style>
</head>
<body>
<div class="container">
<h1>매니저 목록</h1>
<table id="teacher-tbl" class="table table-hover">
<thead>
  <tr>
    <th>번호</th>
    <th>이름</th>
    <th>직급</th>
    <th>팩스</th>
    <th>사진파일경로</th>
  </tr>
</thead>
<tbody>
{{#each data}}
  <tr>
    <td>{{mno}}</td>
    <td><a href="detail.do?no={{mno}}" data-no="{{mno}}">{{name}}</a></td>
    <td>{{posi}}</td>
    <td>{{fax}}</td>
    <td>{{path}}</td>
  </tr>
{{/each}}
</tbody>
</table>

<a href="list.do?pageNo={{prevPageNo}}" class="btn btn-primary" {{disabledPrevBtn}}>이전</a>
<span id="page-no">{{pageNo}}</span>
<a href="list.do?pageNo={{nextPageNo}}" class="btn btn-primary" {{disabledNextBtn}}>다음</a>
<a href="form.do" class="btn btn-primary">추가</a>
</div>

</body>
</html>












<!-- -->
